name: barry_mobile_hud_live
packages:
  - apps/**
  - packages/**

scripts:
  analyze:
    run: flutter analyze
    exec:
      concurrency: 1
      failFast: true

  test:
    run: flutter test
    exec:
      concurrency: 1
      failFast: true
    packageFilters:
      dirExists: test
