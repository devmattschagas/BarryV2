#!/usr/bin/env bash
set -euo pipefail

if command -v gradle >/dev/null 2>&1; then
  exec gradle "$@"
fi

echo "Gradle is required but was not found in PATH." >&2
echo "Install Gradle or regenerate wrapper files with: gradle wrapper" >&2
exit 1
